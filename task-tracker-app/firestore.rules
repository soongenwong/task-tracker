// Firestore Security Rules
// These rules allow anyone to read and write to the tasks collection
// In production, you should implement proper authentication and user-specific rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents in the tasks collection
    // In production, replace with user-specific rules like:
    // match /users/{userId}/tasks/{taskId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }
    match /tasks/{taskId} {
      allow read, write: if true;
    }
  }
}
